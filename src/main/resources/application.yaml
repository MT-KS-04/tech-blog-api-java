spring:
  application:
    name: tech-blog-api
  datasource:
    url: jdbc:mysql://localhost:3306/tech_blog_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8888
  
app:
  environment: development
  log-level: info